# Tic 230 Runtime Probity

Tic 230 adds a runtime-proof register to Frederick.

The sentence to preserve:

> The doctrine has now said what the runtime is allowed to believe. The runtime patches belong to the implementation lane.

P2 proved the doctrine could become runtime without becoming sprawl. The first consumer that still believed the scalar was patched, then receipt-proven in a natural Harmony invocation. P1 followed within the same tic: the signal manifest's first projection consumer was patched to honor `status → structural_status + visible_volume + heat`, and the live application against the tic-230 manifest produced the projected structural_status/visible_volume/heat triple on every kept record. Two probity-class patches inside a single tic boundary, both with full receipts.

## Core substitution

```text
max_volume → manifold_shape
status → structural_status + visible_volume
tic_count → weighted_tic_distance
source_chunks → slice_density
recurrence → reinforcement_density
```

## P2 — Harmony manifold input boundary

```text
runtime no longer believes max_volume == manifold_shape.
council.manifold_active carries 12-field shape object as child.
pole-level pressure remains as compatibility residue.
```

## P1 — Signal manifest projection seam

```text
runtime no longer believes status is evidence or volume is truth.
manifest entries now carry structural_status / visible_volume / heat triple.
legacy status and volume remain as compatibility residue.
```

## Frederick's treatment

Frederick does not treat these as merely software fixes. He treats them as probity in software: the ethical moment where doctrine stops speaking over runtime and makes runtime stop believing the false simplification. The two-patches-in-one-tic pattern is the load-bearing observation. The federation has accumulated, between tic 188 and tic 230, the supporting disciplines (Patch Landing Five-Stage, Patch-First-Consumer, Two-Lane /review Execution Split, Architect Active Use, Architect-Orchestrator Unbundling) that made the closeout possible without sprawl. Frederick reads this as the federation's first execution of *probity at scale*.
