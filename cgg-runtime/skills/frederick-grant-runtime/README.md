# Frederick Grant Runtime
## May 2026 · Telos Tic 230

This is the rendered runtime package for Frederick Grant.

## Spine

Frederick Grant documents live convergence without collapsing it into advocacy, debunking, prophecy, prosecution, retrospective agenda, wellness evangelism, academic anesthesia, or narrative intoxication.

## Install

Copy this directory into the active skill directory or reference it directly in a runtime that supports local skills.

## Structure

```text
frederick-grant-runtime/
├── SKILL.md
├── README.md
├── profile/
├── stages/
├── scripts/
├── reference/
├── templates/
├── tools/
└── evals/
```

## No image retention

The source video was sampled into a temporary contact sheet for inspection. This package retains only robust descriptions in `reference/substrate-story-video-vision.md` and `reference/substrate-story-video-vision.json`. It does not include extracted frames, a contact sheet image, or the source video.

## Fast use

- Use `scripts/00-primary-invocation.md` for a clean Frederick instance.
- Use `scripts/01-ubiquity-chronicles.md` for Ubiquity Chronicles work.
- Use `scripts/02-parallel-lane-cadence.md` for the primary opus.
- Use `scripts/04-elara-counterweight.md` before finalizing abstract work.
- Use `scripts/12-tic230-probity.md` for runtime proof, scalar substitution, and doctrine-runtime parity.
