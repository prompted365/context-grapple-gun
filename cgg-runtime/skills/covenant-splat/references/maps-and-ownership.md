# maps-and-ownership — what locates vs what sequences vs what authorizes

**Never conflate a map-find with a strike-order, or either with admission.**

| Surface | Role | Is NOT |
|---|---|---|
| `audit-logs/governance/NAVIGATION.md` | router-of-routers: the authoritative places and owning structures ("where does X live?") | a sequencer, an admission record |
| io-map | actual producer/consumer + read/write geography | authority over meaning |
| router / route surfaces | correct routing/container surface + direction | covenant semantics |
| the live board (conformation slice) | SEQUENCES: what order, what parallel waves, gated on what | a set of decorated backlog rows |
| the admitted covenant | constrains MEANING and AUTHORITY (target, exclusions, output authority) | derivable from a title, a plan, or route metadata |
| canonical-mount | the harness/provider origination rail supplying bounded intelligence | an authority source |
| homeskillet-csl crates | make the traversal physical (lower, raise, reverse, verify, receipt) | inferrers of topology from the splat field (that join is the missing runtime piece) |
| canonical | durable truth, absorption, terminalization (sole-writer) | a lane any proposer may terminalize into |

Hydration order for the splat's current field: NAVIGATION → io-map → router → repository/runtime state → receipts → OT/heritage scars → authority geometry. **The maps locate its reachable reality; the splat interprets; the covenant authorizes; the receipt proves this time.**

Membrane discipline while hydrating: FIELD surfaces (OT scars, T7 corpus, sibling estates) are EVIDENCE across a read-only membrane — never authority, never written through, never inferred-absent from a canonical index.
