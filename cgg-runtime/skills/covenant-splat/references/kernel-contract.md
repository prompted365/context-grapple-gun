# kernel-contract — pointer and read instructions

**The doctrine body lives at `autonomous_kernel/covenant-splat-fqoq-runtime-spec.md`** (kernel root, sibling-beside-Harmony; status `active`, `runtime_realization: partial`, `missing_runtime_join: splat-conditioned-agentic-covenant-interpretation`). This skill NEVER duplicates it — follow the pointer, do not assume the body.

## Read instructions

1. Read the spec **in full, gapless** on first use in a session (it is a ~20-section runtime contract, not a reference dump). Sections you will exercise most: §5 (covenant lifecycle — four objects never conflated), §6 (CovenantSlice schema), §9 (bounded morphism-proposal contract + prohibitions), §14 (failure taxonomy + resolver + five-status axes), §15 (source/currentness rules — route-metadata demotion, plans-are-not-authority, OT-scars-inform-never-authorize), §17 (acceptance assertions — what `validate-covenant-projection.py` enforces).
2. The verbatim v3 payload at the spec's tail (§20) is a **provenance record**; the structured contract above it is operative. The load-bearing lines are the **v3.1-corrected** five (covenant durable / splat conformation — the splat never re-authors the covenant).
3. Constitutional questions about the spec route to /review — never resolve them inside this skill.

## The one-line orientation

> An approved covenant does not need to prove why it should exist again. It needs to survive intact into an executable, current Reality→Target route.
